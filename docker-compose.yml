services:
  mybot:
    build:
      context: .
      network: host
    ports:
      - "8000:8000"
    env_file:
      - ./.env
    volumes:
      - /home/noboobs/noBS_BotIntegrator/src:/home/noboobs/n/src